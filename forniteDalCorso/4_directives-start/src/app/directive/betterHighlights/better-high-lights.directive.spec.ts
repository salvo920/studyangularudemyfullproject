import { BetterHighLightsDirective } from './better-high-lights.directive';

describe('BetterHighLightsDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHighLightsDirective();
    expect(directive).toBeTruthy();
  });
});
