import { AppHighLightsDirective } from './app-high-lights.directive';

describe('AppHighLightsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHighLightsDirective();
    expect(directive).toBeTruthy();
  });
});
